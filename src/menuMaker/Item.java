package menuMaker;

public class Item {
	String name;

	@Override
	public String toString() {
		return "Item [name=" + name + "]";
	}
	
	//Will probable implement later
	//To make searching for items easier, the only other solution I can think of is text search.
	enum ItemType {
	
	}
}
